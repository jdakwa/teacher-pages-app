// Simple standards implementation for general use
export function getRelevantStandards(state: string, subjectType: string, gradeLevel: string): string[] {
  // For now, return empty array since we're not using state-specific standards
  // This matches the avalern-next implementation for general use
  return [];
}